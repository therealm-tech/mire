# No source declared: the UI prompts for the value, and it is never stored.
#
# `allowed_hosts` is optional and off by default; it is here to show how to pin a
# credential to one host.

# ---
# name: pasted
# kind: token
# allowed_hosts:
#   - models.internal

# A projected service account token, for a real cluster — the file is re-read on
# every call, so rotation is picked up without restarting.
#
# Shipped commented out: a file that declares nothing declares nothing, so this
# sits here as a shape to copy rather than as a provider that cannot resolve.
# Uncomment it and it is live.

# ---
# name: workload-token
# kind: token
# value:
#   file: /var/run/secrets/kubernetes.io/serviceaccount/token

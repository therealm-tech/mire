# You, through your browser — the other half of "what does the pod get?".
#
# Sign in from the auth panel first: until there is a session, a call to this
# server answers `409 not_signed_in` and **nothing is sent**, rather than going
# out half-authenticated and coming back with a confusing 401.

# ---
# name: files-as-me
# url: https://mcp.internal/mcp
# auth: keycloak-user

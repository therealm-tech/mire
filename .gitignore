/target
**/*.rs.bk

# UI build artefacts (embedded into the binary at build time)
/ui/node_modules
/ui/dist

# Where `Attach` writes, when nothing points it elsewhere. Somebody else's files.
/uploads

.DS_Store

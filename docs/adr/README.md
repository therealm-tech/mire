# Architecture decisions

One file per decision, written when it is taken and then left alone: a decision
that is reversed is superseded by a new record rather than edited into shape.
What the system looks like *today* is [ARCHITECTURE.md](../../ARCHITECTURE.md);
these say why it got that way.

| ADR | Decision | Status |
| --- | --- | --- |
| [0001](0001-push-configuration-reloads-to-the-browser.md) | Push configuration reloads to the browser over server-sent events | accepted |
